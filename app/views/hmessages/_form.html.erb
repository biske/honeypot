<%= form_for(@hmessage, html: { class: 'form-signin'}) do |f| %>
  <% if @hmessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hmessage.errors.count, "error") %> prohibited this hmessage from being saved:</h2>

      <ul>
      <% @hmessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.hidden_field :hire_id %>
  </div>

  <div>
    <%= f.text_area :content, cols: 10, rows: 10, placeholder: 'Your message' %>
  </div>
  <div>
    <%= f.submit "Send message", class: 'btn btn-primary' %>
  </div>
<% end %>

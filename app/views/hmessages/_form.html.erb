<%= form_for(@hmessage) do |f| %>
  <% if @hmessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hmessage.errors.count, "error") %> prohibited this hmessage from being saved:</h2>

      <ul>
      <% @hmessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :point_of_view %><br>
    <%= f.text_field :point_of_view %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

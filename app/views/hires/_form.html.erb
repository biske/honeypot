<%= form_for(@hire) do |f| %>
  <% if @hire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hire.errors.count, "error") %> prohibited this hire from being saved:</h2>

      <ul>
      <% @hire.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
